import { css } from 'emotion';

const styles = css`
  display: inline-flex;
  align-self: center;
  
  svg {
    width: 1em;
    height: 1em;
    fill: currentColor;
    top: 0.1em;
    position: relative;
  }
`;

export default styles;